# Database Seeds (Сиды)

## Что такое Seeds?

**Seeds (сиды)** — это начальные данные для заполнения базы данных. Они нужны для:
- Создания базовых записей (роли, статусы, настройки)
- Тестовых данных для разработки
- Заполнения справочников

## Структура проекта

```
src/database/
├── seeds/
│   ├── index.ts           # Главный файл для запуска всех сидов
│   └── roles.seed.ts      # Сид для ролей
└── seeder.ts              # Скрипт для выполнения сидов
```

## Как запустить сиды?

### Локальная разработка:
```bash
npm run seed
```

### Production (на сервере):
```bash
NODE_ENV=production npm run seed
```

## Как работают сиды?

### 1. Идемпотентность (можно запускать много раз)

Сиды проверяют существование записей перед созданием:

```typescript
const existingRole = await Role.findOne({ where: { value: 'normal' } });

if (!existingRole) {
    await Role.create({ value: 'normal', description: '...' });
    console.log('✅ Role created');
} else {
    console.log('⏭️ Role exists, skipping');
}
```

**Что это значит:**
- Можно запускать `npm run seed` сколько угодно раз
- Данные **не дублируются**
- Если роль уже есть — пропускается
- Если роли нет — создаётся

### 2. Порядок выполнения

Важно создавать данные в правильном порядке:

```typescript
// ❌ НЕПРАВИЛЬНО: создаём пользователя до ролей
await usersSeed();  // Ошибка! Ролей ещё нет
await rolesSeed();

// ✅ ПРАВИЛЬНО: сначала роли, потом пользователи
await rolesSeed();  // Создаём роли
await usersSeed();  // Теперь можем назначить роли пользователям
```

**Почему это важно:**
- Пользователь зависит от роли (foreign key)
- Нельзя создать пользователя с ролью, которой нет в БД
- Sequelize выбросит ошибку

## Текущие сиды

### Роли (roles.seed.ts)

Создаёт 3 роли:

| Роль | Описание |
|------|----------|
| `normal` | Обычный пользователь с базовыми правами |
| `superuser` | Суперпользователь с расширенными правами (может управлять пользователями и контентом) |
| `ADMIN` | Администратор с полным доступом к системе |

## Как добавить новый сид?

### Шаг 1: Создать файл сида

```typescript
// src/database/seeds/users.seed.ts
import { User } from '../../users/users.model';
import { Role } from '../../roles/roles.model';
import * as bcrypt from 'bcryptjs';

export const usersSeed = async () => {
    const adminRole = await Role.findOne({ where: { value: 'ADMIN' } });

    const adminUser = {
        email: 'admin@example.com',
        password: await bcrypt.hash('admin123', 5),
        status: 'active'
    };

    const existingUser = await User.findOne({
        where: { email: adminUser.email }
    });

    if (!existingUser && adminRole) {
        const user = await User.create(adminUser);
        await user.$set('roles', [adminRole.id]);
        console.log('✅ Admin user created');
    } else {
        console.log('⏭️ Admin user exists, skipping');
    }
};
```

### Шаг 2: Добавить в index.ts

```typescript
// src/database/seeds/index.ts
import { rolesSeed } from './roles.seed';
import { usersSeed } from './users.seed';  // Новый сид

export const runSeeds = async () => {
    console.log('🌱 Starting database seeding...\n');

    try {
        await rolesSeed();   // Сначала роли
        await usersSeed();   // Потом пользователи (зависят от ролей)

        console.log('\n✅ Database seeding completed successfully!');
    } catch (error) {
        console.error('\n❌ Error during seeding:', error);
        throw error;
    }
};
```

### Шаг 3: Запустить

```bash
npm run seed
```

## Production: Как накатывать сиды только один раз?

### Вариант 1: Ручное управление

```bash
# На production сервере запускаем вручную после деплоя
ssh production-server
cd /var/www/app
NODE_ENV=production npm run seed
```

**Плюсы:**
- Полный контроль
- Видно что происходит

**Минусы:**
- Нужно помнить запускать
- Человеческий фактор

### Вариант 2: CI/CD Pipeline

Добавьте в ваш deploy скрипт:

```yaml
# .github/workflows/deploy.yml (пример для GitHub Actions)
- name: Run database seeds
  run: |
    NODE_ENV=production npm run seed
  env:
    POSTGRES_HOST: ${{ secrets.DB_HOST }}
    POSTGRES_PASSWORD: ${{ secrets.DB_PASSWORD }}
```

**Плюсы:**
- Автоматически при деплое
- Не забудете

**Минусы:**
- Запускается при каждом деплое (но это ок, т.к. сиды идемпотентные)

### Вариант 3: Migrations с флагом

Создать таблицу `seeds_history`:

```typescript
// Проверка: запускался ли этот сид?
const wasRun = await SeedHistory.findOne({ where: { name: 'roles' } });

if (!wasRun) {
    await rolesSeed();
    await SeedHistory.create({ name: 'roles', runAt: new Date() });
}
```

**Плюсы:**
- Точно контролируем что запускалось
- Можно откатывать

**Минусы:**
- Больше кода

## Best Practices

### ✅ DO (Делайте так):

1. **Сиды идемпотентные** — проверяйте существование перед созданием
2. **Порядок важен** — зависимые данные создавайте после базовых
3. **Используйте transactions** для больших сидов
4. **Логируйте результаты** (✅ created, ⏭️ skipped)
5. **Тестируйте на локальной БД** перед production

### ❌ DON'T (Не делайте так):

1. **Не создавайте дубликаты** — всегда проверяйте существование
2. **Не используйте хардкод паролей** в production сидах
3. **Не смешивайте сиды и миграции** — это разные вещи
4. **Не удаляйте данные** в сидах (для этого используйте отдельные скрипты)

## Seeds vs Migrations

| Характеристика | Seeds | Migrations |
|----------------|-------|------------|
| Назначение | Данные | Структура БД (таблицы, колонки) |
| Когда запускать | После миграций | Перед запуском приложения |
| Можно запускать много раз | ✅ Да (идемпотентные) | ❌ Нет (отслеживаются) |
| Откат | Обычно нет | ✅ Да (down migrations) |
| Production | Опционально | ✅ Обязательно |

## Troubleshooting

### Ошибка: "Foreign key constraint failed"

**Причина:** Пытаетесь создать запись со ссылкой на несуществующую запись

**Решение:**
1. Проверьте порядок сидов в `index.ts`
2. Убедитесь что базовые данные (роли) создаются первыми

### Ошибка: "Unique constraint failed"

**Причина:** Пытаетесь создать запись с уже существующим уникальным полем

**Решение:**
1. Добавьте проверку существования перед созданием
2. Используйте `findOrCreate` вместо `create`

### Сиды не запускаются

**Причина:** Проблема с подключением к БД или неправильный NODE_ENV

**Решение:**
1. Проверьте `.env` файл
2. Убедитесь что БД запущена
3. Проверьте переменную NODE_ENV

## Примеры использования

### Пример 1: Заполнение справочника статусов

```typescript
export const statusesSeed = async () => {
    const statuses = ['pending', 'active', 'banned'];

    for (const status of statuses) {
        const exists = await Status.findOne({ where: { name: status } });
        if (!exists) {
            await Status.create({ name: status });
        }
    }
};
```

### Пример 2: Создание тестового пользователя (только для development)

```typescript
export const devUsersSeed = async () => {
    if (process.env.NODE_ENV !== 'development') {
        console.log('⏭️ Skipping dev users (not in development mode)');
        return;
    }

    // Создаём тестового пользователя
    await User.findOrCreate({
        where: { email: 'test@test.com' },
        defaults: {
            password: await bcrypt.hash('test123', 5),
            status: 'active'
        }
    });
};
```

## Заключение

Seeds — это мощный инструмент для управления начальными данными. Правильное использование сидов упрощает развёртывание и тестирование приложения.

**Ключевые моменты:**
- ✅ Идемпотентность (можно запускать много раз)
- ✅ Правильный порядок (зависимости)
- ✅ Проверка существования перед созданием
- ✅ Логирование результатов
